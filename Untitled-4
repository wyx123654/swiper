copy   ：只拷贝最外面这一层的列表，里面的那一层列表没有影响
deepcopy： 完全不同的两个列表

lambda :
    X = [10,-51,15,-24,-61,-51,-68,76]
    按照绝对值大小去进行排序
    sorted(X,key = lambda num:-num if num<0 else num) 返回一个新列表
    X.sort(key = lambda num :abs(num))    直接对X列表进行更改

类方法    和类实例无关，直接在类里面去取东西，用类方法

静态方法  速度最快，和类无关

垃圾收集  每个对象有一个计数器，引用一次计数器就加一
          循环引用的删除，引用计数做不了
          标记清除解决循环引用的问题，分代回收


垃圾回收

__new__

中间件